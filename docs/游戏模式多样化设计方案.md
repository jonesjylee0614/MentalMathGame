# 口算游戏 - 多样化游戏模式设计方案

## 📋 现状分析

### 当前问题
- **单一视觉效果**：所有关卡都是"植物 vs 僵尸"战斗模式
- **答对**：植物🌻发子弹🟢攻击僵尸🧟
- **答错**：僵尸扔石头🟤攻击植物
- **小孩反馈**：虽然题目类型多样,但玩久了觉得枯燥

### 改进目标
1. **视觉多样化**：设计5-8种不同的游戏模式
2. **题型匹配**：不同类型的题目对应不同的游戏效果
3. **渐进式体验**：从简单到复杂,保持新鲜感
4. **保持一致性**：核心玩法不变(答题→反馈→进度推进)

---

## 🎮 游戏模式设计

### 模式1: 战斗模式 (Battle Mode)
**当前已有的模式,保留并优化**

**适用题型**：
- 基础加减法
- 乘除法口诀
- 两位数运算

**视觉元素**：
- 玩家角色：植物🌻 / 战士⚔️ / 机器人🤖
- 敌人：僵尸🧟 / 怪兽👹 / 外星人👾
- 效果：子弹发射、血条、连击数

**优化建议**：
```javascript
// 不同难度使用不同角色组合
const battleThemes = {
  easy: { player: '🌻', enemy: '🐛', bullet: '🟢', attack: '🟤' },
  medium: { player: '⚔️', enemy: '🧟', bullet: '⚡', attack: '🔥' },
  hard: { player: '🤖', enemy: '👾', bullet: '🚀', attack: '💥' }
};
```

---

### 模式2: 建造模式 (Building Mode)
**盖房子、建城堡、搭积木**

**适用题型**：
- 10以内/20以内连加
- 整十数加法
- 相同整数连加

**视觉元素**：
- **答对**：添加一块积木🧱,房子逐渐长高
- **答错**：积木掉落,需要重新搭建
- **进度**：显示建筑完成度(0% → 100%)
- **终点**：完成一座完整的城堡🏰或高楼🏢

**动画效果**：
```css
/* 积木从底部飞入并堆叠 */
@keyframes blockFlyIn {
  from { transform: translateY(200px) scale(0.5); opacity: 0; }
  to { transform: translateY(0) scale(1); opacity: 1; }
}

/* 答错时积木抖动并掉落 */
@keyframes blockFall {
  0% { transform: translateY(0) rotate(0deg); }
  50% { transform: translateY(10px) rotate(5deg); }
  100% { transform: translateY(300px) rotate(45deg); opacity: 0; }
}
```

**数据结构**：
```javascript
gameState: {
  mode: 'building',
  blocks: [], // 已放置的积木
  buildingHeight: 0, // 当前高度
  targetHeight: 20, // 目标高度(题目数量)
  buildingType: 'castle' // castle, tower, bridge
}
```

---

### 模式3: 收集模式 (Collection Mode)
**收集星星、水果、宝石**

**适用题型**：
- 填空题
- 单位换算
- 比较大小

**视觉元素**：
- 场景：果园🍎🍊🍓 / 宝石矿⭐💎✨ / 海底🐠🐚🦀
- **答对**：收集一个物品到篮子里
- **答错**：物品从篮子里掉出一个
- **进度**：篮子填满度

**动画效果**：
```javascript
// 答对：物品飞入篮子
function collectItem(item) {
  const itemEl = document.createElement('div');
  itemEl.textContent = item.emoji;
  itemEl.className = 'collect-item';
  itemEl.style.setProperty('--target-x', basketX);
  itemEl.style.setProperty('--target-y', basketY);
  // 贝塞尔曲线飞行路径
  itemEl.style.animation = 'collectFly 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94)';
}

// 答错：物品从篮子弹出
function dropItem() {
  const lastItem = basket.lastChild;
  lastItem.style.animation = 'itemDrop 0.6s ease-in';
}
```

**主题配置**：
```javascript
const collectionThemes = {
  fruit: { 
    items: ['🍎', '🍊', '🍓', '🍌', '🍇'],
    background: '果园背景',
    container: '🧺 篮子'
  },
  gem: {
    items: ['💎', '⭐', '✨', '🔮', '💠'],
    background: '宝石矿洞',
    container: '⛏️ 矿车'
  },
  ocean: {
    items: ['🐠', '🐚', '🦀', '🐙', '🦈'],
    background: '海底世界',
    container: '🪣 水桶'
  }
};
```

---

### 模式4: 赛跑模式 (Racing Mode)
**跑步比赛、赛车、游泳**

**适用题型**：
- 20以内加减混合
- 连减题
- 有余数的除法

**视觉元素**：
- 赛道：从左到右的跑道🏃→🏁
- **我方选手**：🏃小人 / 🚗赛车 / 🏊游泳者
- **对手**：🐢乌龟 / 🐇兔子 / 🤖机器人
- **答对**：我方前进一大步
- **答错**：对手前进一步
- **终点**：先到达终点线者获胜

**实现逻辑**：
```javascript
gameState: {
  mode: 'racing',
  playerPosition: 0, // 0-100%
  opponentPosition: 0,
  trackLength: 100,
  playerSpeed: 5, // 每次答对前进5%
  opponentSpeed: 2 // 每次答错对手前进2%
}

function onCorrectAnswer() {
  playerPosition = Math.min(100, playerPosition + playerSpeed);
  // 加速线条动画
  playerEl.style.transform = `translateX(${playerPosition}%)`;
  playerEl.classList.add('running');
  if (playerPosition >= 100) gameOver('win');
}

function onWrongAnswer() {
  opponentPosition = Math.min(100, opponentPosition + opponentSpeed);
  opponentEl.style.transform = `translateX(${opponentPosition}%)`;
  if (opponentPosition >= 100) gameOver('lose');
}
```

---

### 模式5: 解密模式 (Puzzle Mode)
**拼图、解锁、寻宝**

**适用题型**：
- 含小括号的混合运算
- 三数加减混合
- 复杂运算

**视觉元素**：
- 画面：模糊的图片逐渐清晰 / 锁🔒逐渐打开 / 宝箱🎁
- **答对**：拼图块出现/图片清晰度+10%/解锁一层
- **答错**：无变化(不倒退)
- **完成**：揭示完整的图片/打开宝箱

**实现逻辑**：
```javascript
gameState: {
  mode: 'puzzle',
  revealProgress: 0, // 0-100%
  puzzleType: 'image', // image, lock, chest
  revealPerCorrect: 5 // 每次答对揭示5%
}

// 拼图模式
function revealPuzzle() {
  revealProgress += revealPerCorrect;
  puzzleContainer.style.clipPath = `inset(0 ${100-revealProgress}% 0 0)`;
  puzzleOverlay.style.opacity = 1 - (revealProgress / 100);
}

// 解锁模式
function unlockLayer() {
  const lockLayers = document.querySelectorAll('.lock-layer');
  const currentLayer = Math.floor(revealProgress / 10);
  if (lockLayers[currentLayer]) {
    lockLayers[currentLayer].classList.add('unlocked');
  }
}
```

---

### 模式6: 种植模式 (Farming Mode)
**种花、养树、照顾植物**

**适用题型**：
- 乘法口诀
- 表内除法
- 长时间关卡(30+题)

**视觉元素**：
- 场景：花园🌱→🌿→🌻→🌺
- **答对**：植物生长一个阶段
- **答错**：害虫🐛出现,需要额外答题驱赶
- **完成**：花园开满鲜花

**生长阶段**：
```javascript
const growthStages = [
  { stage: 0, emoji: '🌱', name: '种子' },
  { stage: 1, emoji: '🌿', name: '发芽' },
  { stage: 2, emoji: '🪴', name: '幼苗' },
  { stage: 3, emoji: '🌻', name: '开花' },
  { stage: 4, emoji: '🌺', name: '盛开' }
];

function growPlant(correctCount) {
  const stage = Math.floor(correctCount / (totalQuestions / growthStages.length));
  plantEl.textContent = growthStages[stage].emoji;
  stageLabel.textContent = growthStages[stage].name;
}
```

---

### 模式7: 探险模式 (Adventure Mode)
**走迷宫、爬山、探索地图**

**适用题型**：
- 整千数/整百数运算
- 混合运算
- 综合题型

**视觉元素**：
- 地图：起点🏠 → 路径🛤️ → 终点🏆
- 每个节点是一道题
- **答对**：前进到下一个节点
- **答错**：停留原地,出现路障
- **特殊点**：宝箱(额外分数)、陷阱(更难的题)

**地图数据**：
```javascript
const adventureMap = [
  { id: 1, type: 'start', emoji: '🏠', x: 10, y: 50 },
  { id: 2, type: 'normal', emoji: '🛤️', x: 25, y: 40 },
  { id: 3, type: 'treasure', emoji: '🎁', x: 40, y: 35 },
  { id: 4, type: 'trap', emoji: '⚠️', x: 55, y: 45 },
  { id: 5, type: 'checkpoint', emoji: '🏁', x: 70, y: 30 },
  { id: 6, type: 'boss', emoji: '👹', x: 85, y: 50 },
  { id: 7, type: 'end', emoji: '🏆', x: 95, y: 50 }
];

// 玩家移动
function movePlayer(fromNode, toNode) {
  const path = calculatePath(fromNode, toNode);
  animateMovement(playerEl, path, 800); // 0.8秒移动动画
}
```

---

### 模式8: 防守模式 (Defense Mode)
**守卫城堡、太空防御**

**适用题型**：
- 连除
- 除加混合
- 快速反应类题目

**视觉元素**：
- 敌人从右侧源源不断走来👾👾👾
- 每个敌人携带一道题
- **答对**：击退敌人
- **答错/超时**：敌人到达城堡,城堡扣血
- **失败**：城堡被攻破

**实现逻辑**：
```javascript
gameState: {
  mode: 'defense',
  castleHP: 100,
  waves: [], // 敌人波次
  currentWave: 0,
  enemiesDefeated: 0
}

function spawnEnemy(question) {
  const enemy = {
    id: question.id,
    question: question,
    position: 100, // 从右侧100%开始
    speed: 1, // 每秒移动1%
    hp: 1
  };
  
  // 敌人自动前进
  const interval = setInterval(() => {
    enemy.position -= enemy.speed;
    enemyEl.style.right = `${enemy.position}%`;
    
    if (enemy.position <= 0) {
      clearInterval(interval);
      damageCastle(10);
    }
  }, 100);
}
```

---

## 🎨 关卡与模式映射方案

### 基础入门关卡 (1-26关)
| 关卡ID | 关卡名称 | 推荐模式 | 理由 |
|--------|---------|---------|------|
| add_1_5, sub_1_5 | 5以内加减 | **建造模式** | 简单题适合看到明确的进度积累 |
| add_1_10, sub_1_10 | 10以内加减 | **收集模式-水果** | 孩子喜欢收集水果的成就感 |
| fill_1_10, fill_1_20 | 填空题 | **解密模式** | 填空类似解谜 |
| make10 | 凑10练习 | **收集模式-拼10** | 特殊的收集10个星星玩法 |
| add_carry_20, sub_borrow_20 | 进位/退位 | **战斗模式-简单** | 有挑战性,适合战斗 |
| tens_*, two_* | 整十数/两位数 | **赛跑模式** | 多题连续,适合竞速 |
| mix_1, mix_2 | 综合题 | **探险模式** | 综合题有探索感 |

### 进阶拓展关卡 (27-58关)
| 关卡ID | 关卡名称 | 推荐模式 | 理由 |
|--------|---------|---------|------|
| chain_add_*, chain_sub_* | 三数连加/连减 | **赛跑模式** | 连续运算像接力赛 |
| mul_6, mul_9 | 乘法口诀 | **种植模式** | 需要重复练习,看植物生长有成就感 |
| div_6, div_9 | 除法 | **防守模式** | 快速反应,击退敌人 |
| muldiv_* | 乘除混合 | **战斗模式-中级** | 有挑战,战斗模式合适 |
| unit_* | 单位换算 | **收集模式-宝石** | 换算类似整理分类 |
| time_compare | 大小比较 | **赛跑模式** | 比较速度天然匹配赛跑 |
| k_1000_*, hundreds_* | 大数运算 | **建造模式-摩天大楼** | 大数字建高楼 |
| paren_mix | 括号混合 | **解密模式-保险箱** | 最难的题配最高级解密 |

### 挑战关卡
| 关卡ID | 关卡名称 | 推荐模式 | 理由 |
|--------|---------|---------|------|
| div_remainder | 有余数除法 | **探险模式** | 复杂运算配探险 |
| multi_mul_10, multi_div_10 | 连乘/连除 | **战斗模式-BOSS战** | 高难度用终极战斗 |

---

## 🏗️ 框架架构设计

### 设计原则

#### 1. 插件化 (Plugin Architecture)
- 每个游戏模式是一个独立的插件
- 插件之间互不干扰，可独立开发、测试、部署
- 支持动态加载和卸载

#### 2. 低耦合高内聚
- 核心引擎与游戏模式解耦
- 通过标准接口通信
- 模式内部高度自治

#### 3. 开放封闭原则
- 对扩展开放：新增模式无需修改核心代码
- 对修改封闭：现有模式不受新模式影响

#### 4. 单一职责
- GameEngine：题目逻辑、计分、生命周期
- GameMode：视觉表现、动画效果、用户交互
- GameModeManager：模式注册、创建、管理

#### 5. 依赖倒置
- 核心依赖抽象接口，不依赖具体实现
- 通过工厂模式创建实例

---

### 核心架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         PlayPage                            │
│                   (UI 容器和事件协调)                         │
└───────────────────┬─────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
┌───────▼────────┐      ┌──────▼──────────┐
│  GameEngine    │      │ GameModeManager │
│  (核心逻辑)     │◄─────┤  (模式管理器)    │
└───────┬────────┘      └──────┬──────────┘
        │                      │
        │                      │ creates
        │                      │
        │               ┌──────▼──────────┐
        │               │ IGameMode       │
        │               │ (抽象接口)       │
        │               └──────┬──────────┘
        │                      │ implements
        │                      │
        └──────────┬───────────┴────────┬─────────┬─────────┐
                   │                    │         │         │
            ┌──────▼─────┐      ┌──────▼──┐  ┌──▼───┐  ┌──▼────┐
            │BattleMode  │      │Building │  │Racing│  │...    │
            │  Plugin    │      │  Mode   │  │ Mode │  │       │
            └────────────┘      └─────────┘  └──────┘  └───────┘
```

---

### 核心接口设计

#### IGameMode - 游戏模式标准接口

```typescript
// frontend/src/lib/gameModes/IGameMode.ts

/**
 * 游戏模式生命周期钩子
 */
export interface IGameMode {
  /** 模式唯一标识 */
  readonly id: string;
  
  /** 模式名称 */
  readonly name: string;
  
  /** 模式版本 (用于兼容性检查) */
  readonly version: string;

  /** 
   * 初始化模式
   * @param config - 关卡配置
   * @param context - 游戏上下文（引擎引用、事件总线等）
   */
  init(config: GameModeConfig, context: GameContext): void;

  /**
   * 游戏开始
   * @param totalQuestions - 总题目数
   */
  onGameStart(totalQuestions: number): void;

  /**
   * 答题正确回调
   * @param question - 当前题目
   * @param snapshot - 游戏快照
   */
  onCorrectAnswer(question: Question, snapshot: GameSnapshot): void;

  /**
   * 答题错误回调
   * @param question - 当前题目
   * @param snapshot - 游戏快照
   * @param userAnswer - 用户答案
   * @param correctAnswer - 正确答案
   */
  onWrongAnswer(
    question: Question, 
    snapshot: GameSnapshot, 
    userAnswer: string, 
    correctAnswer: string
  ): void;

  /**
   * 游戏状态更新（每秒触发）
   * @param snapshot - 游戏快照
   */
  onUpdate(snapshot: GameSnapshot): void;

  /**
   * 游戏结束
   * @param result - 游戏结果
   */
  onGameEnd(result: GameResult): void;

  /**
   * 销毁模式（清理资源）
   */
  destroy(): void;

  /**
   * 获取当前模式状态（用于渲染）
   */
  getState(): GameModeState;

  /**
   * 获取渲染组件
   */
  getComponent(): React.ComponentType<GameModeRenderProps>;
}

/**
 * 模式配置
 */
export interface GameModeConfig {
  /** 关卡信息 */
  level: Level;
  
  /** 模式特定配置 */
  modeConfig?: Record<string, any>;
  
  /** 用户设置 */
  settings: Settings;
}

/**
 * 游戏上下文
 */
export interface GameContext {
  /** 引擎引用 */
  engine: GameEngine;
  
  /** 事件总线 */
  eventBus: EventBus;
  
  /** 音效播放器 */
  soundPlayer: SoundPlayer;
}

/**
 * 模式状态（由各模式自定义）
 */
export interface GameModeState {
  /** 模式ID */
  mode: string;
  
  /** 自定义状态数据 */
  data: Record<string, any>;
}

/**
 * 渲染属性
 */
export interface GameModeRenderProps {
  /** 模式状态 */
  state: GameModeState;
  
  /** 游戏快照 */
  snapshot: GameSnapshot;
  
  /** 当前题目 */
  question: Question | null;
  
  /** 反馈信息 */
  feedback: { correct: boolean; expected?: string } | null;
}
```

#### GameModeManager - 模式管理器

```typescript
// frontend/src/lib/gameModes/GameModeManager.ts

/**
 * 游戏模式管理器
 * 负责注册、创建、管理游戏模式实例
 */
export class GameModeManager {
  private static instance: GameModeManager;
  
  /** 已注册的模式工厂 */
  private factories = new Map<string, GameModeFactory>();
  
  /** 当前激活的模式实例 */
  private currentMode: IGameMode | null = null;

  private constructor() {
    // 私有构造函数，实现单例
  }

  static getInstance(): GameModeManager {
    if (!GameModeManager.instance) {
      GameModeManager.instance = new GameModeManager();
    }
    return GameModeManager.instance;
  }

  /**
   * 注册游戏模式
   * @param factory - 模式工厂
   */
  register(factory: GameModeFactory): void {
    if (this.factories.has(factory.modeId)) {
      console.warn(`游戏模式 ${factory.modeId} 已注册，将被覆盖`);
    }
    this.factories.set(factory.modeId, factory);
    console.log(`✅ 游戏模式 [${factory.modeId}] 注册成功`);
  }

  /**
   * 批量注册模式
   */
  registerAll(factories: GameModeFactory[]): void {
    factories.forEach(factory => this.register(factory));
  }

  /**
   * 创建游戏模式实例
   * @param modeId - 模式ID
   * @param config - 配置
   * @param context - 上下文
   */
  create(modeId: string, config: GameModeConfig, context: GameContext): IGameMode {
    const factory = this.factories.get(modeId);
    
    if (!factory) {
      console.warn(`游戏模式 [${modeId}] 未注册，使用默认模式 [battle]`);
      return this.createDefault(config, context);
    }

    try {
      const mode = factory.create(config, context);
      console.log(`🎮 创建游戏模式实例: ${mode.name} (${mode.id}@${mode.version})`);
      return mode;
    } catch (error) {
      console.error(`创建模式 [${modeId}] 失败:`, error);
      return this.createDefault(config, context);
    }
  }

  /**
   * 创建默认模式（战斗模式）
   */
  private createDefault(config: GameModeConfig, context: GameContext): IGameMode {
    const defaultFactory = this.factories.get('battle');
    if (!defaultFactory) {
      throw new Error('默认游戏模式 [battle] 未注册！');
    }
    return defaultFactory.create(config, context);
  }

  /**
   * 激活模式
   */
  activate(mode: IGameMode): void {
    if (this.currentMode) {
      this.deactivate();
    }
    this.currentMode = mode;
  }

  /**
   * 停用当前模式
   */
  deactivate(): void {
    if (this.currentMode) {
      this.currentMode.destroy();
      this.currentMode = null;
    }
  }

  /**
   * 获取当前激活的模式
   */
  getCurrentMode(): IGameMode | null {
    return this.currentMode;
  }

  /**
   * 获取所有已注册的模式
   */
  getRegisteredModes(): string[] {
    return Array.from(this.factories.keys());
  }

  /**
   * 检查模式是否已注册
   */
  isRegistered(modeId: string): boolean {
    return this.factories.has(modeId);
  }
}

/**
 * 游戏模式工厂接口
 */
export interface GameModeFactory {
  /** 模式ID */
  readonly modeId: string;
  
  /** 工厂名称 */
  readonly name: string;
  
  /** 创建模式实例 */
  create(config: GameModeConfig, context: GameContext): IGameMode;
}
```

---

## 🔧 技术实现方案

### 1. 数据结构扩展

在 Level 类型中添加 `gameMode` 字段:

```typescript
// frontend/src/lib/types.ts
export type GameMode = 
  | 'battle'      // 战斗
  | 'building'    // 建造
  | 'collection'  // 收集
  | 'racing'      // 赛跑
  | 'puzzle'      // 解密
  | 'farming'     // 种植
  | 'adventure'   // 探险
  | 'defense';    // 防守

export interface Level {
  id: string;
  category: string;
  name: string;
  desc: string;
  generator: QuestionGenerator;
  count: number;
  timeSec: number;
  difficulty: number;
  hp?: { player: number; monster: number };
  gameMode?: GameMode; // 新增：指定游戏模式
  modeConfig?: Record<string, any>; // 新增：模式特定配置
}
```

### 2. 基础抽象类（可选）

为了进一步简化开发，提供一个抽象基类：

```typescript
// frontend/src/lib/gameModes/BaseGameMode.ts

import { IGameMode, GameModeConfig, GameContext, GameModeState, GameModeRenderProps } from './IGameMode';
import { Question, GameSnapshot, GameResult } from '../types';
import React from 'react';

/**
 * 游戏模式抽象基类
 * 提供默认实现和工具方法，子类可按需覆盖
 */
export abstract class BaseGameMode implements IGameMode {
  abstract readonly id: string;
  abstract readonly name: string;
  readonly version: string = '1.0.0';

  protected config!: GameModeConfig;
  protected context!: GameContext;
  protected state: Record<string, any> = {};

  init(config: GameModeConfig, context: GameContext): void {
    this.config = config;
    this.context = context;
    this.state = this.initState();
  }

  /**
   * 初始化模式特定状态（子类实现）
   */
  protected abstract initState(): Record<string, any>;

  onGameStart(totalQuestions: number): void {
    // 默认实现：记录总题数
    this.state.totalQuestions = totalQuestions;
  }

  onCorrectAnswer(question: Question, snapshot: GameSnapshot): void {
    // 默认实现：播放音效
    this.context.soundPlayer.play('correct');
  }

  onWrongAnswer(
    question: Question, 
    snapshot: GameSnapshot, 
    userAnswer: string, 
    correctAnswer: string
  ): void {
    // 默认实现：播放音效
    this.context.soundPlayer.play('wrong');
  }

  onUpdate(snapshot: GameSnapshot): void {
    // 默认实现：更新通用状态
    this.state.currentQuestion = snapshot.questionIndex + 1;
    this.state.timeLeft = snapshot.timeLeft;
  }

  onGameEnd(result: GameResult): void {
    // 默认实现：清空状态
  }

  destroy(): void {
    // 默认实现：清理状态
    this.state = {};
  }

  getState(): GameModeState {
    return {
      mode: this.id,
      data: { ...this.state }
    };
  }

  abstract getComponent(): React.ComponentType<GameModeRenderProps>;

  /**
   * 工具方法：触发自定义事件
   */
  protected emit(eventName: string, payload: any): void {
    this.context.eventBus.emit(`gameMode:${this.id}:${eventName}`, payload);
  }

  /**
   * 工具方法：监听自定义事件
   */
  protected on(eventName: string, handler: (payload: any) => void): () => void {
    return this.context.eventBus.on(`gameMode:${this.id}:${eventName}`, handler);
  }

  /**
   * 工具方法：更新状态并触发渲染
   */
  protected setState(updates: Partial<Record<string, any>>): void {
    this.state = { ...this.state, ...updates };
    this.emit('stateChange', this.getState());
  }
}
```

### 3. 具体实现示例

#### 示例1: 战斗模式（现有模式改造）

```typescript
// frontend/src/lib/gameModes/modes/BattleMode.ts

import { BaseGameMode } from '../BaseGameMode';
import { Question, GameSnapshot, GameResult } from '../../types';
import { BattleModeComponent } from '../../../components/gameModes/BattleModeComponent';

export class BattleMode extends BaseGameMode {
  readonly id = 'battle';
  readonly name = '战斗模式';

  protected initState() {
    return {
      plantAttacking: false,
      zombieAttacking: false,
      zombieDamaged: false,
      playerDamaged: false,
      projectiles: {
        plant: [],
        zombie: []
      }
    };
  }

  onCorrectAnswer(question: Question, snapshot: GameSnapshot): void {
    super.onCorrectAnswer(question, snapshot);
    
    // 触发植物攻击动画
    this.setState({
      plantAttacking: true
    });

    // 发射子弹
    const bulletId = `bullet-${Date.now()}`;
    this.state.projectiles.plant.push(bulletId);
    this.emit('plantBulletFired', { bulletId });

    // 600ms后僵尸受伤
    setTimeout(() => {
      this.setState({ zombieDamaged: true });
      this.emit('zombieDamaged', {});
    }, 600);

    // 2秒后重置动画
    setTimeout(() => {
      this.setState({
        plantAttacking: false,
        zombieDamaged: false
      });
      this.clearProjectiles('plant');
    }, 2000);
  }

  onWrongAnswer(
    question: Question, 
    snapshot: GameSnapshot, 
    userAnswer: string, 
    correctAnswer: string
  ): void {
    super.onWrongAnswer(question, snapshot, userAnswer, correctAnswer);

    // 触发僵尸攻击动画
    this.setState({
      zombieAttacking: true
    });

    // 发射石头
    const bulletId = `rock-${Date.now()}`;
    this.state.projectiles.zombie.push(bulletId);
    this.emit('zombieBulletFired', { bulletId });

    // 500ms后植物受伤
    setTimeout(() => {
      this.setState({ playerDamaged: true });
      this.emit('playerDamaged', {});
    }, 500);

    // 1.2秒后重置动画
    setTimeout(() => {
      this.setState({
        zombieAttacking: false,
        playerDamaged: false
      });
      this.clearProjectiles('zombie');
    }, 1200);
  }

  private clearProjectiles(type: 'plant' | 'zombie'): void {
    this.setState({
      projectiles: {
        ...this.state.projectiles,
        [type]: []
      }
    });
  }

  getComponent() {
    return BattleModeComponent;
  }
}

// 工厂
export const BattleModeFactory = {
  modeId: 'battle',
  name: '战斗模式工厂',
  create: (config, context) => {
    const mode = new BattleMode();
    mode.init(config, context);
    return mode;
  }
};
```

#### 示例2: 建造模式（新模式）

```typescript
// frontend/src/lib/gameModes/modes/BuildingMode.ts

import { BaseGameMode } from '../BaseGameMode';
import { Question, GameSnapshot, GameResult } from '../../types';
import { BuildingModeComponent } from '../../../components/gameModes/BuildingModeComponent';

export class BuildingMode extends BaseGameMode {
  readonly id = 'building';
  readonly name = '建造模式';

  protected initState() {
    return {
      blocks: [],
      buildingHeight: 0,
      targetHeight: 0,
      buildingType: this.config.modeConfig?.buildingType || 'tower',
      lastBlockShake: false
    };
  }

  onGameStart(totalQuestions: number): void {
    super.onGameStart(totalQuestions);
    this.setState({
      targetHeight: totalQuestions
    });
  }

  onCorrectAnswer(question: Question, snapshot: GameSnapshot): void {
    super.onCorrectAnswer(question, snapshot);

    // 添加一块积木
    const block = {
      id: `block-${Date.now()}`,
      type: 'block',
      emoji: '🧱',
      level: this.state.buildingHeight
    };

    this.state.blocks.push(block);
    this.setState({
      buildingHeight: this.state.buildingHeight + 1
    });

    // 触发积木飞入动画
    this.emit('blockAdded', { block });

    // 检查是否完成建筑
    if (this.state.buildingHeight >= this.state.targetHeight) {
      this.emit('buildingComplete', {});
      this.playCompletionAnimation();
    }
  }

  onWrongAnswer(
    question: Question, 
    snapshot: GameSnapshot, 
    userAnswer: string, 
    correctAnswer: string
  ): void {
    super.onWrongAnswer(question, snapshot, userAnswer, correctAnswer);

    // 触发最后一块积木抖动
    this.setState({
      lastBlockShake: true
    });

    setTimeout(() => {
      this.setState({
        lastBlockShake: false
      });
    }, 600);
  }

  private playCompletionAnimation(): void {
    // 完成动画：烟花、彩带等
    this.emit('fireworks', {});
  }

  getComponent() {
    return BuildingModeComponent;
  }
}

// 工厂
export const BuildingModeFactory = {
  modeId: 'building',
  name: '建造模式工厂',
  create: (config, context) => {
    const mode = new BuildingMode();
    mode.init(config, context);
    return mode;
  }
};
```

### 4. 组件示例

```tsx
// frontend/src/components/gameModes/BuildingModeComponent.tsx

import React, { useEffect } from 'react';
import { GameModeRenderProps } from '../../lib/gameModes/IGameMode';
import styles from '../../styles/gameModes/BuildingMode.module.css';

export const BuildingModeComponent: React.FC<GameModeRenderProps> = ({
  state,
  snapshot,
  question,
  feedback
}) => {
  const { blocks, buildingHeight, targetHeight, buildingType, lastBlockShake } = state.data;

  const progressPercent = targetHeight > 0 
    ? Math.round((buildingHeight / targetHeight) * 100) 
    : 0;

  return (
    <div className={styles.buildingScene}>
      {/* 目标建筑轮廓 */}
      <div className={styles.targetBuilding}>
        {buildingType === 'castle' && '🏰'}
        {buildingType === 'tower' && '🏢'}
        {buildingType === 'bridge' && '🌉'}
      </div>

      {/* 进度指示 */}
      <div className={styles.buildingProgress}>
        <div className={styles.progressBar}>
          <div 
            className={styles.progressFill} 
            style={{ height: `${progressPercent}%` }}
          />
        </div>
        <span className={styles.progressLabel}>
          {buildingHeight} / {targetHeight}
        </span>
      </div>

      {/* 积木堆叠区 */}
      <div className={styles.blocksContainer}>
        {blocks.map((block: any, index: number) => (
          <div
            key={block.id}
            className={`${styles.block} ${
              index === blocks.length - 1 && lastBlockShake ? styles.shake : ''
            }`}
            style={{
              bottom: `${index * 40}px`,
              animationDelay: `${index * 0.1}s`
            }}
          >
            {block.emoji}
          </div>
        ))}
      </div>

      {/* 反馈动画 */}
      {feedback && (
        <div className={`${styles.feedback} ${feedback.correct ? styles.correct : styles.wrong}`}>
          {feedback.correct ? '✅ 添加一层！' : '❌ 再试一次！'}
        </div>
      )}
    </div>
  );
};
```

### 5. 注册与初始化流程

```typescript
// frontend/src/lib/gameModes/registry.ts

import { GameModeManager } from './GameModeManager';
import { BattleModeFactory } from './modes/BattleMode';
import { BuildingModeFactory } from './modes/BuildingMode';
import { CollectionModeFactory } from './modes/CollectionMode';
// ... 导入其他模式工厂

/**
 * 注册所有游戏模式
 * 应用启动时调用一次
 */
export function registerGameModes(): void {
  const manager = GameModeManager.getInstance();

  // 注册所有模式
  manager.registerAll([
    BattleModeFactory,
    BuildingModeFactory,
    CollectionModeFactory,
    // ... 其他模式
  ]);

  console.log('✅ 游戏模式注册完成');
  console.log('已注册模式:', manager.getRegisteredModes());
}
```

```typescript
// frontend/src/main.tsx

import { registerGameModes } from './lib/gameModes/registry';

// 应用启动时注册所有游戏模式
registerGameModes();

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
```

### 6. PlayPage 集成

```typescript
// frontend/src/routes/PlayPage.tsx

import { useEffect, useState, useMemo } from 'react';
import { GameModeManager } from '../lib/gameModes/GameModeManager';
import { IGameMode } from '../lib/gameModes/IGameMode';
import { Game } from '../lib/engine';
import { EventBus } from '../lib/eventBus';

export const PlayPage = () => {
  const { levelId } = useParams();
  const level = useMemo(() => (levelId ? findLevel(levelId) : null), [levelId]);
  
  const [gameMode, setGameMode] = useState<IGameMode | null>(null);
  const [modeState, setModeState] = useState<GameModeState | null>(null);
  const [snapshot, setSnapshot] = useState<GameSnapshot | null>(null);
  const [question, setQuestion] = useState<Question | null>(null);
  const [feedback, setFeedback] = useState<{ correct: boolean; expected?: string } | null>(null);

  useEffect(() => {
    if (!level) return;

    // 创建事件总线
    const eventBus = new EventBus();
    
    // 创建音效播放器
    const soundPlayer = {
      play: (sound: string) => playSound(sound)
    };

    // 创建游戏模式实例
    const modeId = level.gameMode || 'battle';
    const manager = GameModeManager.getInstance();
    
    const mode = manager.create(
      modeId,
      {
        level,
        modeConfig: level.modeConfig,
        settings
      },
      {
        engine: Game,
        eventBus,
        soundPlayer
      }
    );

    manager.activate(mode);
    setGameMode(mode);

    // 监听游戏引擎事件并转发给游戏模式
    const unsubUpdate = Game.on('update', (snap) => {
      setSnapshot(snap);
      mode.onUpdate(snap);
      setModeState(mode.getState());
    });

    const unsubQuestion = Game.on('question', (q) => {
      setQuestion(q);
      setFeedback(null);
    });

    const unsubFeedback = Game.on('feedback', (fb) => {
      setFeedback(fb);
      const currentQuestion = Game.getCurrentQuestion();
      const currentSnapshot = snapshot || Game.snapshot();
      
      if (fb.correct && currentQuestion) {
        mode.onCorrectAnswer(currentQuestion, currentSnapshot);
      } else if (!fb.correct && currentQuestion) {
        mode.onWrongAnswer(
          currentQuestion,
          currentSnapshot,
          '', // 用户答案
          fb.expected || ''
        );
      }
      
      setModeState(mode.getState());
    });

    const unsubFinish = Game.on('finish', (result) => {
      mode.onGameEnd(result);
      // ... 处理结果
    });

    // 启动游戏
    Game.init(level);
    mode.onGameStart(level.count);
    Game.start();
    setModeState(mode.getState());

    // 清理
    return () => {
      unsubUpdate();
      unsubQuestion();
      unsubFeedback();
      unsubFinish();
      manager.deactivate();
      Game.reset();
    };
  }, [level]);

  if (!level || !gameMode || !modeState) {
    return <div>加载中...</div>;
  }

  // 获取模式专用组件
  const ModeComponent = gameMode.getComponent();

  return (
    <div className={styles.wrapper}>
      {/* 顶部状态栏 - 通用 */}
      <header className={`glass-card ${styles.topBar}`}>
        {/* ... 现有顶部栏代码 ... */}
      </header>

      {/* 游戏区域 - 由模式组件渲染 */}
      <section className={styles.gameArea}>
        <ModeComponent 
          state={modeState}
          snapshot={snapshot!}
          question={question}
          feedback={feedback}
        />
      </section>

      {/* 题目和输入区 - 通用 */}
      <main className={styles.stage}>
        {/* ... 现有题目和输入框代码 ... */}
      </main>

      {/* 底部状态栏 - 通用 */}
      <footer className={`glass-card ${styles.bottomPanel}`}>
        {/* ... 现有底部栏代码 ... */}
      </footer>
    </div>
  );
};
```

### 7. 事件总线实现

```typescript
// frontend/src/lib/eventBus.ts

type EventHandler = (payload: any) => void;

/**
 * 简单的事件总线实现
 * 用于游戏模式内部通信
 */
export class EventBus {
  private events: Map<string, Set<EventHandler>> = new Map();

  /**
   * 监听事件
   */
  on(eventName: string, handler: EventHandler): () => void {
    if (!this.events.has(eventName)) {
      this.events.set(eventName, new Set());
    }
    this.events.get(eventName)!.add(handler);

    // 返回取消监听的函数
    return () => {
      this.events.get(eventName)?.delete(handler);
    };
  }

  /**
   * 触发事件
   */
  emit(eventName: string, payload?: any): void {
    const handlers = this.events.get(eventName);
    if (handlers) {
      handlers.forEach(handler => handler(payload));
    }
  }

  /**
   * 监听一次性事件
   */
  once(eventName: string, handler: EventHandler): () => void {
    const wrapper = (payload: any) => {
      handler(payload);
      this.events.get(eventName)?.delete(wrapper);
    };
    return this.on(eventName, wrapper);
  }

  /**
   * 清除所有监听器
   */
  clear(): void {
    this.events.clear();
  }
}
```

---

## 📁 项目目录结构

```
frontend/
├── src/
│   ├── lib/
│   │   ├── gameModes/
│   │   │   ├── IGameMode.ts              # 核心接口定义
│   │   │   ├── BaseGameMode.ts           # 抽象基类
│   │   │   ├── GameModeManager.ts        # 模式管理器
│   │   │   ├── registry.ts               # 模式注册中心
│   │   │   └── modes/                    # 具体模式实现
│   │   │       ├── BattleMode.ts         # 战斗模式
│   │   │       ├── BuildingMode.ts       # 建造模式
│   │   │       ├── CollectionMode.ts     # 收集模式
│   │   │       ├── RacingMode.ts         # 赛跑模式
│   │   │       ├── PuzzleMode.ts         # 解密模式
│   │   │       ├── FarmingMode.ts        # 种植模式
│   │   │       ├── AdventureMode.ts      # 探险模式
│   │   │       ├── DefenseMode.ts        # 防守模式
│   │   │       └── index.ts              # 统一导出
│   │   ├── eventBus.ts                   # 事件总线
│   │   ├── engine.ts                     # 游戏引擎
│   │   ├── types.ts                      # 类型定义
│   │   └── ...
│   ├── components/
│   │   ├── gameModes/                    # 模式专用组件
│   │   │   ├── BattleModeComponent.tsx
│   │   │   ├── BuildingModeComponent.tsx
│   │   │   ├── CollectionModeComponent.tsx
│   │   │   ├── RacingModeComponent.tsx
│   │   │   ├── PuzzleModeComponent.tsx
│   │   │   ├── FarmingModeComponent.tsx
│   │   │   ├── AdventureModeComponent.tsx
│   │   │   └── DefenseModeComponent.tsx
│   │   └── ...
│   ├── styles/
│   │   ├── gameModes/                    # 模式专用样式
│   │   │   ├── BattleMode.module.css
│   │   │   ├── BuildingMode.module.css
│   │   │   ├── CollectionMode.module.css
│   │   │   ├── RacingMode.module.css
│   │   │   ├── PuzzleMode.module.css
│   │   │   ├── FarmingMode.module.css
│   │   │   ├── AdventureMode.module.css
│   │   │   └── DefenseMode.module.css
│   │   ├── animations/                   # 动画样式
│   │   │   ├── battle.css
│   │   │   ├── building.css
│   │   │   └── ...
│   │   └── ...
│   └── routes/
│       └── PlayPage.tsx                  # 游戏页面（集成）
└── ...
```

---

## 👥 多人协作开发指南

### 1. 开发角色分工

#### 核心开发者（1人）
**负责**：
- 框架设计和实现
- IGameMode、BaseGameMode、GameModeManager
- PlayPage 集成改造
- 代码审查

**产出**：
- 核心接口和基类
- 完整的文档和示例
- CI/CD 流程

#### 模式开发者（2-8人）
**负责**：
- 独立开发一个或多个游戏模式
- 实现模式逻辑类 + React组件 + CSS样式
- 编写单元测试

**产出**：
- 完整的模式插件（类 + 组件 + 样式）
- 模式使用文档
- 测试用例

#### UI/UX 设计师（1人）
**负责**：
- 各模式视觉设计
- 动画效果设计
- 素材准备

**产出**：
- 设计稿
- 动画原型
- 资源文件

---

### 2. 开发流程

#### Phase 1: 框架搭建（核心开发者，3天）
1. ✅ 定义 IGameMode 接口
2. ✅ 实现 BaseGameMode 基类
3. ✅ 实现 GameModeManager
4. ✅ 实现 EventBus
5. ✅ 改造 PlayPage
6. ✅ 编写开发文档和示例

#### Phase 2: 战斗模式改造（核心开发者，2天）
1. ✅ 将现有战斗逻辑迁移到 BattleMode 类
2. ✅ 提取 BattleModeComponent 组件
3. ✅ 验证框架可用性
4. ✅ 编写接入教程

#### Phase 3: 并行开发新模式（模式开发者，1-2周）
- **开发者A**：BuildingMode + CollectionMode
- **开发者B**：RacingMode + PuzzleMode
- **开发者C**：FarmingMode + AdventureMode
- **开发者D**：DefenseMode

#### Phase 4: 集成和测试（全员，3天）
1. 合并所有模式
2. 为关卡分配模式
3. 全量测试
4. Bug 修复

#### Phase 5: 优化和发布（全员，2天）
1. 性能优化
2. 动画优化
3. 用户测试
4. 发布上线

---

### 3. 开发规范

#### 命名规范
```typescript
// 模式类名：{ModeName}Mode
export class BattleMode extends BaseGameMode { }

// 工厂对象：{ModeName}ModeFactory
export const BattleModeFactory = { ... };

// 组件名：{ModeName}ModeComponent
export const BattleModeComponent: React.FC<GameModeRenderProps> = () => { };

// 样式文件：{ModeName}Mode.module.css
```

#### 文件位置
```
模式逻辑类：frontend/src/lib/gameModes/modes/{ModeName}Mode.ts
React组件：frontend/src/components/gameModes/{ModeName}ModeComponent.tsx
CSS样式：frontend/src/styles/gameModes/{ModeName}Mode.module.css
```

#### Git 分支策略
```
main                    - 稳定版本
develop                 - 开发主分支
feature/game-mode-framework  - 框架开发分支
feature/mode-battle     - 战斗模式分支
feature/mode-building   - 建造模式分支
feature/mode-collection - 收集模式分支
...
```

#### Commit 规范
```
feat: 实现建造模式基础功能
fix: 修复战斗模式动画卡顿问题
refactor: 重构BaseGameMode生命周期
style: 优化收集模式UI样式
docs: 更新游戏模式开发文档
test: 添加赛跑模式单元测试
```

---

### 4. 新模式开发 Checklist

#### ✅ 步骤1: 创建模式类
```typescript
// 文件: frontend/src/lib/gameModes/modes/YourMode.ts

import { BaseGameMode } from '../BaseGameMode';

export class YourMode extends BaseGameMode {
  readonly id = 'your-mode-id';
  readonly name = '你的模式名';

  protected initState() {
    return {
      // 初始化你的状态
    };
  }

  // 实现生命周期钩子
  onCorrectAnswer(question, snapshot) {
    super.onCorrectAnswer(question, snapshot);
    // 你的逻辑
  }

  // ...

  getComponent() {
    return YourModeComponent;
  }
}

export const YourModeFactory = {
  modeId: 'your-mode-id',
  name: '你的模式工厂',
  create: (config, context) => {
    const mode = new YourMode();
    mode.init(config, context);
    return mode;
  }
};
```

#### ✅ 步骤2: 创建React组件
```tsx
// 文件: frontend/src/components/gameModes/YourModeComponent.tsx

import React from 'react';
import { GameModeRenderProps } from '../../lib/gameModes/IGameMode';
import styles from '../../styles/gameModes/YourMode.module.css';

export const YourModeComponent: React.FC<GameModeRenderProps> = ({
  state,
  snapshot,
  question,
  feedback
}) => {
  return (
    <div className={styles.container}>
      {/* 你的UI */}
    </div>
  );
};
```

#### ✅ 步骤3: 创建样式文件
```css
/* 文件: frontend/src/styles/gameModes/YourMode.module.css */

.container {
  /* 你的样式 */
}

@keyframes yourAnimation {
  /* 你的动画 */
}
```

#### ✅ 步骤4: 注册模式
```typescript
// 文件: frontend/src/lib/gameModes/registry.ts

import { YourModeFactory } from './modes/YourMode';

export function registerGameModes(): void {
  const manager = GameModeManager.getInstance();
  manager.registerAll([
    // ... 其他工厂
    YourModeFactory,  // 添加你的工厂
  ]);
}
```

#### ✅ 步骤5: 分配给关卡
```typescript
// 文件: frontend/src/lib/levels.ts

export const LEVELS: Level[] = [
  {
    id: 'your-level-id',
    name: '你的关卡',
    // ... 其他字段
    gameMode: 'your-mode-id',  // 指定你的模式
    modeConfig: {
      // 模式特定配置
    }
  },
  // ...
];
```

#### ✅ 步骤6: 测试
- [ ] 单元测试：模式逻辑正确性
- [ ] 集成测试：与引擎集成正常
- [ ] UI测试：组件渲染正确
- [ ] 动画测试：动画流畅无卡顿
- [ ] 兼容性测试：多浏览器测试

#### ✅ 步骤7: 文档
- [ ] 编写模式说明文档
- [ ] 添加配置示例
- [ ] 记录已知问题

---

### 5. 代码审查要点

#### 模式类审查
- [ ] 是否继承自 BaseGameMode
- [ ] 是否正确实现所有生命周期钩子
- [ ] 状态管理是否合理
- [ ] 是否有内存泄漏（setTimeout、事件监听）
- [ ] 错误处理是否完善

#### 组件审查
- [ ] props类型是否正确
- [ ] 是否使用CSS Modules
- [ ] 是否有性能问题（避免过度渲染）
- [ ] 动画是否流畅
- [ ] 是否响应式设计

#### 样式审查
- [ ] 是否使用CSS Modules
- [ ] 是否遵循BEM命名规范
- [ ] 是否有魔法数字（建议用CSS变量）
- [ ] 动画是否有GPU加速（transform、opacity）
- [ ] 是否兼容主流浏览器

---

## 🧪 测试策略

### 单元测试示例

```typescript
// frontend/src/lib/gameModes/modes/__tests__/BuildingMode.test.ts

import { BuildingMode } from '../BuildingMode';
import { GameModeConfig, GameContext } from '../../IGameMode';

describe('BuildingMode', () => {
  let mode: BuildingMode;
  let mockConfig: GameModeConfig;
  let mockContext: GameContext;

  beforeEach(() => {
    mode = new BuildingMode();
    mockConfig = {
      level: { /* mock level */ },
      settings: { /* mock settings */ }
    };
    mockContext = {
      engine: { /* mock engine */ },
      eventBus: { /* mock eventBus */ },
      soundPlayer: { /* mock soundPlayer */ }
    };
    mode.init(mockConfig, mockContext);
  });

  test('should initialize with correct state', () => {
    const state = mode.getState();
    expect(state.data.blocks).toEqual([]);
    expect(state.data.buildingHeight).toBe(0);
  });

  test('should add block on correct answer', () => {
    const mockQuestion = { /* mock question */ };
    const mockSnapshot = { /* mock snapshot */ };
    
    mode.onCorrectAnswer(mockQuestion, mockSnapshot);
    
    const state = mode.getState();
    expect(state.data.buildingHeight).toBe(1);
    expect(state.data.blocks).toHaveLength(1);
  });

  test('should trigger shake on wrong answer', () => {
    const mockQuestion = { /* mock question */ };
    const mockSnapshot = { /* mock snapshot */ };
    
    mode.onWrongAnswer(mockQuestion, mockSnapshot, '5', '10');
    
    const state = mode.getState();
    expect(state.data.lastBlockShake).toBe(true);
  });
});
```

---

## 🚀 性能优化建议

### 1. 懒加载模式
```typescript
// 使用动态import延迟加载模式
const BattleModeFactory = {
  modeId: 'battle',
  name: '战斗模式工厂',
  create: async (config, context) => {
    const { BattleMode } = await import('./modes/BattleMode');
    const mode = new BattleMode();
    mode.init(config, context);
    return mode;
  }
};
```

### 2. 动画性能
```css
/* 使用GPU加速的属性 */
.block {
  /* ✅ 好 - GPU加速 */
  transform: translateY(0);
  opacity: 1;
  will-change: transform, opacity;
  
  /* ❌ 避免 - 触发重排 */
  /* top: 0;
  height: 100px; */
}
```

### 3. 状态更新优化
```typescript
// 使用批量更新减少渲染次数
this.setState({
  plantAttacking: true,
  zombieDamaged: false,
  score: this.state.score + 10
});  // 只触发一次渲染

// 而不是
this.setState({ plantAttacking: true });  // 触发渲染
this.setState({ zombieDamaged: false });  // 触发渲染
this.setState({ score: this.state.score + 10 });  // 触发渲染
```

---

## 📚 参考资料

### 设计模式
- **策略模式** (Strategy Pattern): 游戏模式的切换
- **工厂模式** (Factory Pattern): 模式实例的创建
- **观察者模式** (Observer Pattern): 事件总线
- **单例模式** (Singleton Pattern): GameModeManager

### 架构原则
- **SOLID原则**
- **依赖倒置**
- **控制反转 (IoC)**
- **插件架构**

---

## 🎯 预期收益

### 开发效率
- ✅ 核心框架一次开发，多人并行开发模式
- ✅ 新增模式无需修改核心代码
- ✅ 模式间完全隔离，降低Bug影响范围
- ✅ 标准化接口降低学习成本

### 代码质量
- ✅ 清晰的职责划分
- ✅ 高内聚低耦合
- ✅ 易于测试和维护
- ✅ 代码复用率高

### 用户体验
- ✅ 8种不同游戏模式
- ✅ 流畅的动画效果
- ✅ 无缝的模式切换
- ✅ 丰富的视觉体验

### 可扩展性
- ✅ 轻松添加新模式
- ✅ 支持模式版本管理
- ✅ 支持模式热更新（未来）
- ✅ 支持第三方模式（未来）

---

### 5. 游戏引擎改造（可选）

注意：由于采用了插件架构，游戏引擎**无需大幅改造**。引擎只需专注于核心逻辑（题目、计分、计时），视觉表现完全由游戏模式负责。

如果需要引擎支持模式切换通知：

```typescript
// frontend/src/lib/engine.ts (可选扩展)

export class GameEngine {
  // ... 现有代码保持不变

  // 可选：添加模式钩子
  private modeHooks?: {
    onCorrect?: (question: Question, snapshot: GameSnapshot) => void;
    onWrong?: (question: Question, snapshot: GameSnapshot) => void;
  };

  setModeHooks(hooks: typeof this.modeHooks) {
    this.modeHooks = hooks;
  }

  submit(rawAnswer: string) {
    // ... 现有判分逻辑
    
    if (correct) {
      // ... 现有逻辑
      this.modeHooks?.onCorrect?.(question, this.snapshot());
    } else {
      // ... 现有逻辑
      this.modeHooks?.onWrong?.(question, this.snapshot());
    }
  }
}
  
```

但推荐使用 **事件监听方式**（如 PlayPage 集成示例），更符合解耦原则。

---

## 🎬 实施步骤总结

### 阶段1: 核心框架搭建（3天）

**文件清单**：
```
新增文件：
├── frontend/src/lib/gameModes/
│   ├── IGameMode.ts              ✨ 核心接口
│   ├── BaseGameMode.ts           ✨ 抽象基类
│   ├── GameModeManager.ts        ✨ 管理器
│   ├── registry.ts               ✨ 注册中心
│   └── modes/
│       └── index.ts              ✨ 导出
├── frontend/src/lib/eventBus.ts  ✨ 事件总线
└── frontend/src/components/gameModes/
    └── (待添加)

修改文件：
├── frontend/src/lib/types.ts     🔧 添加 GameMode 类型
├── frontend/src/main.tsx         🔧 注册游戏模式
└── frontend/src/routes/PlayPage.tsx  🔧 集成管理器
```

**验收标准**：
- [x] 所有接口定义完成并通过TypeScript检查
- [x] GameModeManager 单元测试通过
- [x] EventBus 单元测试通过
- [x] 文档齐全，包含使用示例

---

### 阶段2: 战斗模式迁移（2天）

**任务**：
1. 创建 `BattleMode.ts` 类
2. 创建 `BattleModeComponent.tsx` 组件
3. 创建 `BattleMode.module.css` 样式
4. 将 PlayPage 中的战斗逻辑迁移到新架构
5. 验证功能完全一致

**验收标准**：
- [x] 战斗模式功能与原版一致
- [x] 所有动画效果正常
- [x] 无性能退化
- [x] 代码覆盖率 > 80%

---

### 阶段3: 新模式开发（1-2周）

**并行开发任务分配**：

| 开发者 | 模式1 | 模式2 | 预计工时 |
|-------|------|------|---------|
| A | BuildingMode | CollectionMode | 5天 |
| B | RacingMode | PuzzleMode | 5天 |
| C | FarmingMode | AdventureMode | 6天 |
| D | DefenseMode | - | 3天 |

**每个模式的开发checklist**：
- [ ] 模式类实现（1天）
- [ ] React组件实现（1天）
- [ ] CSS动画实现（0.5天）
- [ ] 单元测试（0.5天）
- [ ] 文档编写（0.5天）
- [ ] 代码审查和修改（0.5天）

---

### 阶段4: 集成测试（3天）

**任务**：
1. 合并所有模式分支
2. 为所有58个关卡分配合适的游戏模式
3. 全量测试（每个模式至少测试3个关卡）
4. 修复集成Bug
5. 性能优化

**测试清单**：
- [ ] 所有模式正常加载
- [ ] 模式切换无闪烁
- [ ] 动画流畅（FPS > 30）
- [ ] 无内存泄漏
- [ ] 移动端适配正常
- [ ] 无控制台错误

---

### 阶段5: 发布上线（2天）

**任务**：
1. 最终用户测试（儿童试玩）
2. 性能优化
3. 文档完善
4. 版本发布

---

## 🎓 开发示例：5分钟创建新模式

### 完整示例：炸弹拆除模式

```typescript
// 1️⃣ 模式类 (frontend/src/lib/gameModes/modes/BombMode.ts)
import { BaseGameMode } from '../BaseGameMode';
import { Question, GameSnapshot } from '../../types';
import { BombModeComponent } from '../../../components/gameModes/BombModeComponent';

export class BombMode extends BaseGameMode {
  readonly id = 'bomb';
  readonly name = '拆弹模式';

  protected initState() {
    return {
      wires: ['red', 'blue', 'green', 'yellow'],
      cutWires: [],
      bombTimer: 10,
      defused: false
    };
  }

  onCorrectAnswer(question: Question, snapshot: GameSnapshot): void {
    super.onCorrectAnswer(question, snapshot);
    
    // 剪断一根线
    const wire = this.state.wires[this.state.cutWires.length];
    this.state.cutWires.push(wire);
    this.setState({ cutWires: this.state.cutWires });
    this.emit('wireCut', { wire });

    // 所有线都剪断则拆弹成功
    if (this.state.cutWires.length >= this.state.wires.length) {
      this.setState({ defused: true });
      this.emit('bombDefused', {});
    }
  }

  onWrongAnswer(question: Question, snapshot: GameSnapshot, userAnswer: string, correctAnswer: string): void {
    super.onWrongAnswer(question, snapshot, userAnswer, correctAnswer);
    
    // 答错时炸弹倒计时加速
    this.setState({ bombTimer: Math.max(5, this.state.bombTimer - 2) });
    this.emit('bombShake', {});
  }

  onUpdate(snapshot: GameSnapshot): void {
    super.onUpdate(snapshot);
    // 根据剩余时间更新炸弹危险等级
    const dangerLevel = snapshot.timeLeft < 30 ? 'critical' : 
                       snapshot.timeLeft < 60 ? 'warning' : 'safe';
    this.setState({ dangerLevel });
  }

  getComponent() {
    return BombModeComponent;
  }
}

export const BombModeFactory = {
  modeId: 'bomb',
  name: '拆弹模式工厂',
  create: (config, context) => {
    const mode = new BombMode();
    mode.init(config, context);
    return mode;
  }
};
```

```tsx
// 2️⃣ React组件 (frontend/src/components/gameModes/BombModeComponent.tsx)
import React from 'react';
import { GameModeRenderProps } from '../../lib/gameModes/IGameMode';
import styles from '../../styles/gameModes/BombMode.module.css';

export const BombModeComponent: React.FC<GameModeRenderProps> = ({
  state,
  snapshot,
  feedback
}) => {
  const { wires, cutWires, bombTimer, defused, dangerLevel } = state.data;

  return (
    <div className={`${styles.bombScene} ${styles[dangerLevel]}`}>
      {/* 炸弹主体 */}
      <div className={`${styles.bomb} ${defused ? styles.defused : ''}`}>
        💣
      </div>

      {/* 倒计时 */}
      <div className={styles.timer}>
        <div className={styles.timerDisplay}>{bombTimer}s</div>
      </div>

      {/* 电线 */}
      <div className={styles.wires}>
        {wires.map((wire: string, index: number) => (
          <div
            key={wire}
            className={`${styles.wire} ${styles[wire]} ${
              cutWires.includes(wire) ? styles.cut : ''
            }`}
          >
            {cutWires.includes(wire) ? '✂️' : '━━━'}
          </div>
        ))}
      </div>

      {/* 反馈 */}
      {feedback && (
        <div className={`${styles.feedback} ${feedback.correct ? styles.success : styles.error}`}>
          {feedback.correct ? '✅ 剪对了！' : '❌ 炸弹加速了！'}
        </div>
      )}

      {/* 成功提示 */}
      {defused && (
        <div className={styles.defusedBanner}>
          🎉 拆弹成功！
        </div>
      )}
    </div>
  );
};
```

```css
/* 3️⃣ 样式 (frontend/src/styles/gameModes/BombMode.module.css) */
.bombScene {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 2rem;
  gap: 2rem;
  background: linear-gradient(135deg, #1a1a2e, #16213e);
  border-radius: 12px;
}

.bomb {
  font-size: 6rem;
  animation: bombIdle 2s ease-in-out infinite;
}

.bomb.defused {
  animation: defuseSuccess 1s ease-out;
}

.safe { background: linear-gradient(135deg, #1a1a2e, #16213e); }
.warning { background: linear-gradient(135deg, #ff6b35, #f7931e); }
.critical { background: linear-gradient(135deg, #e63946, #d00000); animation: shake 0.5s infinite; }

.timer {
  font-size: 3rem;
  font-weight: bold;
  color: #ff4444;
  text-shadow: 0 0 20px rgba(255, 68, 68, 0.8);
}

.wires {
  display: flex;
  gap: 1rem;
}

.wire {
  font-size: 2rem;
  transition: all 0.3s ease;
}

.wire.cut {
  opacity: 0.3;
  transform: scale(0.8);
}

@keyframes bombIdle {
  0%, 100% { transform: rotate(-5deg); }
  50% { transform: rotate(5deg); }
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-10px); }
  75% { transform: translateX(10px); }
}

@keyframes defuseSuccess {
  0% { transform: scale(1); }
  50% { transform: scale(1.5) rotate(360deg); }
  100% { transform: scale(1); }
}

.feedback {
  padding: 1rem 2rem;
  border-radius: 8px;
  font-size: 1.5rem;
  font-weight: bold;
  animation: fadeIn 0.3s ease-out;
}

.feedback.success {
  background: #4caf50;
  color: white;
}

.feedback.error {
  background: #f44336;
  color: white;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(-20px); }
  to { opacity: 1; transform: translateY(0); }
}

.defusedBanner {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 3rem;
  color: #4caf50;
  text-shadow: 0 0 40px rgba(76, 175, 80, 0.8);
  animation: celebrate 2s ease-out;
}

@keyframes celebrate {
  0% { transform: translate(-50%, -50%) scale(0); opacity: 0; }
  50% { transform: translate(-50%, -50%) scale(1.2); opacity: 1; }
  100% { transform: translate(-50%, -50%) scale(1); opacity: 1; }
}
```

```typescript
// 4️⃣ 注册 (frontend/src/lib/gameModes/registry.ts)
import { BombModeFactory } from './modes/BombMode';

export function registerGameModes(): void {
  const manager = GameModeManager.getInstance();
  manager.registerAll([
    BattleModeFactory,
    BuildingModeFactory,
    BombModeFactory,  // ✅ 添加新模式
    // ...
  ]);
}
```

```typescript
// 5️⃣ 使用 (frontend/src/lib/levels.ts)
export const LEVELS: Level[] = [
  {
    id: 'mul_urgent',
    category: '乘法',
    name: '紧急拆弹',
    desc: '答对题目拆除炸弹！',
    // ...
    gameMode: 'bomb',  // ✅ 指定拆弹模式
    modeConfig: {
      bombType: 'time',
      difficulty: 'hard'
    }
  }
];
```

**完成！🎉** 

仅需 **5个文件**，总计 **~150行代码**，就创建了一个全新的游戏模式。

---

## ✅ 总结

### 框架核心优势

1. **插件化架构**
   - 游戏模式完全解耦，可独立开发、测试、发布
   - 新增模式无需修改核心代码

2. **标准化接口**
   - IGameMode 定义清晰的生命周期
   - 所有模式遵循相同的开发规范

3. **工厂模式**
   - GameModeManager 统一管理模式创建
   - 支持懒加载、版本控制、降级策略

4. **事件驱动**
   - 通过 EventBus 实现模式内部通信
   - 解耦UI和逻辑

5. **多人协作友好**
   - 清晰的职责划分
   - 完整的开发文档和示例
   - 标准化的开发流程

### 关键决策

| 决策点 | 选择 | 理由 |
|-------|------|------|
| 架构模式 | 插件化 | 易扩展、高内聚低耦合 |
| 接口设计 | 生命周期钩子 | 清晰、标准化 |
| 状态管理 | 模式独立状态 | 避免全局状态污染 |
| 组件渲染 | 动态组件 | 灵活、按需加载 |
| 样式隔离 | CSS Modules | 避免样式冲突 |
| 事件通信 | EventBus | 解耦、灵活 |

### 预期成果

- ✅ **8种游戏模式** 提升用户体验
- ✅ **多人并行开发** 加速开发进度
- ✅ **高质量代码** 易维护、易扩展
- ✅ **完整文档** 降低学习成本

### 下一步

1. **立即开始**：搭建核心框架（3天）
2. **战斗模式迁移**：验证框架可行性（2天）
3. **并行开发新模式**：多人协作（1-2周）
4. **集成测试发布**：全量测试和上线（3天）

---

**预计总工时**: 15-20个工作日
**预计完成日期**: 3-4周

🚀 **Let's Build It!**